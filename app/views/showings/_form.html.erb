<%= form_with(model: showing) do |form| %>
  <% if showing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(showing.errors.count, "error") %> prohibited this showing from being saved:</h2>

      <ul>
        <% showing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cinema_id %>
    <%= form.text_field :cinema_id %>
  </div>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.text_field :movie_id %>
  </div>

  <div class="field">
    <%= form.label :show_time %>
    <%= form.datetime_select :show_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
